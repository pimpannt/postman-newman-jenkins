{
  "name": "postman-newman-jenkins",
  "version": "1.0.0",
  "description": "Project for storing Postman tests",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/jsonplaceholder.com.postman_collection.json --reporters cli,junit --reporter-json-export newman.json"
   
  },
  "author": "pimpan",
  "dependencies": {
    "newman": "^3.10.0"
  }
}

